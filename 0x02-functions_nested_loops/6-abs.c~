#include "holberton.h"
/**
 * _abs - writes the absolute value
 * @int: The character to print absolute value
 *
 * Return: 0
 */
int _abs(int)
{
	int n;

	if (n > 0)
	{
		_putchar(n + '0');
	}
	else if (n < 0)
	{
		_putchar(n * -1);
	}
	else
	{
		_putchar('0');
	}

	return (0);
}
