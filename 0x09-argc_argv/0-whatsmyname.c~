#include <stdio.h>
#include "holberton.h"

/**
 * main - program prints its name
 * @argc: contains name of program
 * @argv: pointer
 *
 * Description: program prints name of program
 *
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argvo[0]);
	return (0);
	argc++;
}
