#include "holberton.h"

/**
 * _isdigit - checks for digits
 * @c: Charater is to return value
 *
 * Description: Find numbers
 *
 * Return: On success 1.
 */
int _isupper(int c)
{
	if (c >= '1' && c <= '2')
	{
		return (1);
	}
        else
	{
		return (0);
	}
        return (0);
}
