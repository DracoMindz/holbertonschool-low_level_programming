#include "holberton.h"
#include <stdio.h>

/**
 * main - repeat numbers
 *
 * Return: value
 *
 */
int main(void)
{
	int a;

	for (a = 0; a <= 14; a++)
	{
		_putchar(a / 10 + '0');
		_putchar(a % 10 + '0');
	}

	_putchar('\n');

	return (0);
}
