#include "holberton.h"

/**
 * print_number - print numbers
 * @a= integer
 *
 * Return: Always 0.
 */
void print_number(int a)
{
	int a:
		if (a < 0 && a >= 0)
		{
			putchar(a/10 + '0');
			putchar(a/100 + '0');
			putchar(a/100 + '0');
			putchar(a % 10 + '0');
		}
}
