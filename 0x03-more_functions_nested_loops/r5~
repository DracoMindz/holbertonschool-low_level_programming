#include "holberton.h"
#include <stdio.h>

/**
 * main - repeat numbers
 *
 * Return: value
 *
 */
int main(void)
{
	int a;

	for (a = 0; a <= 14; a++)
	{
		putchar(a / 10 + '0');
		putchar(a % 10 + '0');
	}

	putchar('\n');

	return (0);
}
