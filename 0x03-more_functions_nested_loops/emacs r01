#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
	int a;
	int b;

	    for (a = 'a'; a <= 'z'; a++)
	    {
	    putchar(a);
	    for (b = 'A'; b <= 'Z'; b++)
	    {
	    putchar(b);
	    }

	    return (0);
}