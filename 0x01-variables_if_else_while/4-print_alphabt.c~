/**
 * main - lowercase alphabets
 *
 * Description: print lower case alphabets
 *
 * Return: 0
 */

#include <studio.h>
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	{
		putchar(a);
	}
	putchar('\n');

	return (0)
}
