#include <stdio.h>
/**
 * main - print numbers
 *
 * Description: Print single digit numbers
 *
 * Return: 0
 */

int main(void)
{
	int z
		for (z = 'z'; z>= 'a'; z--)
		{
			putchar(z);
		}
	putchar('\n');

	return(0)
}
