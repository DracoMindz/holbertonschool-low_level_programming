#include <stdio.h>
/**
 * main - print lowercase, then uppercase
 *
 * Description: Will print upper and lower cases together
 *
 * Return: 0
 */

int main (void)
{
	int ch;
	for (ch = '0'; ch <= '9', ch++
		{ putchar(ch);
		}
		     putchar('\n');
	     Return (0);
}
