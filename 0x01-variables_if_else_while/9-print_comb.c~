#include <stdio.h>

/**
 * main - print combinations
 *
 * Description: Will print single digit combinations
 *
 * Return: 0
 */

int main(void)
{
	int a;
	for (a = 0; a <= 9; a++)
	{
		putchar(a);
		if (n != 9)
		{
			putchar(',');
			putchar(" ");
		}
	}
	putchar('\n');

	return(0);
}
